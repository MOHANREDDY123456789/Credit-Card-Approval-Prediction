[project]
name = "credit-card-approval-prediction"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = [
    "flask==3.0.3",
    "numpy==1.26.4",
    "pandas==2.2.2",
    "scikit-learn==1.5.0",
    "xgboost==2.0.3",
]

[tool.vercel]
entrypoint = "Flask.app:app"
